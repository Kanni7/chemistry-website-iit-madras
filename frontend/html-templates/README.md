# Initial HTML Templates

This folder contains the main HTML pages that were created for the initial folder structure of the Chemistry Department website.

Since the frontend is being built using React (Vite), these raw HTML files are stored here as a reference. As the development progresses, these HTML pages and their structures should be converted into reusable React components and pages inside the `src/components` and `src/pages` directories.
